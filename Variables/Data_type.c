/*
In C, every piece of data in your program has a specific type. 
Data types define what kind of data a variable can hold and how much memory it needs. 
Let's look at the basic data types in C:
Create an integer variable: int age;
This declares a variable named age that can hold whole numbers.
age = 25;
Here are the primary data types in C:

int number = 42;        // Integer (whole number)
float price = 10.5f;    // Floating-point (decimal)
double pi = 3.14159;    // Double precision floating-point
char grade = 'A';       // Single character

int stores whole numbers. 
float stores decimal numbers —the f suffix (e.g. 10.5f) tells C to treat the value as a float rather than a double.
double stores decimal numbers with greater precision than float.
char stores a single character enclosed in single quotes.
 
  Code:*/
#include<stdio.h>
int main(){
  int price=120;
}
